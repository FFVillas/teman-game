/**
 * Background art for the auth panel's "image"/"carousel"/"tint" variants —
 * see AuthShell's `panelVariant` prop, tried out via /login?bg=carousel
 * etc. Drop the actual splash-art files at these paths to see them.
 *
 * Prefer each publisher's official splash/key art (or press kit) over fan
 * art here — this panel reads as "TemanGame's own branding," which is a
 * more prominent use than a small game-cover thumbnail elsewhere on the
 * site, so it's worth being more careful about sourcing.
 */
export const authPanelImages: string[] = [
  "/auth/panel-valorant.png",
  "/auth/panel-mlbb.png",
  "/auth/panel-lol.jpg",
];
